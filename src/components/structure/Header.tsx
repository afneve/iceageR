import NavItems from './NavItems';
import ProgressBar from '../ProgressBar';


const Header = () => {
	return (
		<header>
			<nav>
				<NavItems />
			</nav>
			<ProgressBar />
		</header>
	);
}

export default Header;
